# Startup gate reused by every app service: don't boot until the one-shot
# migrations container has finished (it in turn waits for Postgres to be
# healthy). Without this the services race the DB and die with "connection
# refused" on a cold start.
x-depends-on-db: &depends-on-db
  db-migrations:
    condition: service_completed_successfully

services:
  postgres:
      image: postgres:15
      environment:
        POSTGRES_USER: user
        POSTGRES_PASSWORD: password
        POSTGRES_DB: market_data_db
      ports:
        - "5433:5432"
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U user -d market_data_db"]
        interval: 3s
        timeout: 3s
        retries: 5
  db-migrations:
    build: ./db
    # "Drogi Dockerze, cokolwiek Twój program stworzy lub zmieni w swoim wewnętrznym folderze /app,
    # natychmiast skopiuj to jeden do jednego do folderu db na moim laptopie".
    volumes:
      - ./db:/app
      - ./shared:/app/shared
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
          postgres:
            condition: service_healthy
  market-data-service:
    build: ./services/market-data-service
    ports:
      - "8001:8001"
    volumes:
      - ./shared:/app/shared
    environment:
      - TICK_INTERVAL_MS=1500
      - DATABASE_URL=${DATABASE_URL}
    depends_on: *depends-on-db
  pricing-service:
    build: ./services/pricing-service
    ports:
      - "8002:8002"
    volumes:
      - ./shared:/app/shared
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on: *depends-on-db
  # monitoring-service:
  #   build: ./services/monitoring-service
  #   ports:
  #     - "8003:8003"
  books-service:
    build: ./services/books-service
    ports:
      - "8004:8004"
    volumes:
      - ./shared:/app/shared
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on: *depends-on-db
  blotter-service:
    build: ./services/blotter-service
    ports:
      - "8006:8006"
    volumes:
      - ./shared:/app/shared
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on: *depends-on-db
  trade-generation-service:
    build: ./services/trade-generation-service
    ports:
      - "8007:8007"
    volumes:
      - ./shared:/app/shared
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - TRADE_GENERATION_INTERVAL_MS=${TRADE_GENERATION_INTERVAL_MS}
    depends_on: *depends-on-db
  trade-action-service:
    build: ./services/trade-action-service
    ports:
      - "8080:8080"
    volumes:
      - ./shared:/app/shared
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on: *depends-on-db
    